# TDW Video Deliverables Update Report

## Status

No new TDW v2 generated videos were added.

## Reason

The mild-only plan was fixed and validated, but actual TDW generation was blocked by the display/GPU routing gate. The existing deliverables under `local_assets/reports/tdw_video_deliverables/` remain valid as indexes of prior existing assets.

## New Category

The intended category is:

`New TDW v2 warmup_mild generated samples`

It will be populated after the first accepted generated sample passes validation.

## Current Note For Presentation

The old Physion/TDW moving-camera clean GT sample should be described as stress/test material, not warmup data, because its camera motion is too strong and the foreground can disappear.

