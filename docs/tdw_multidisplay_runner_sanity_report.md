# TDW Multi-display Runner Sanity Report

Status: runner sanity deferred after display blocker.

The repository has `scripts/34_run_tdw_multidisplay_generation.sh` and related generation utilities in the worktree, but no TDW multi-display smoke is allowed until at least two non-llvmpipe NVIDIA displays are verified.
