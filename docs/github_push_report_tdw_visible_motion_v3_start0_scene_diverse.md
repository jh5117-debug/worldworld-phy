# GitHub push report: TDW visible-motion v3 start0 scene-diverse

Branch:

`physion-tdw-visible-motion-v3-start0-scene-diverse`

Remote:

`ssh://git@ssh.github.com:443/jh5117-debug/worldworld-phy.git`

Primary commit:

`c7adc54` - `Add start0 scene-diverse visible-motion TDW v3 review set`

Push status:

Pushed successfully to origin after retrying with approved network escalation because the first local sandboxed push failed DNS resolution for `ssh.github.com`.

Scope pushed:

- TDW v3 code/config updates;
- validation/conversion/final docs;
- PRD/plan/spec/stage2 updates.

Excluded from Git:

- `local_assets/`;
- generated HDF5 / H5;
- generated MP4;
- NPY / NPZ;
- large logs;
- weights / latents / checkpoints / LoRA.
